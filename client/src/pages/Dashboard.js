import React from "react";
import { useMutation } from '@apollo/client'


export default function Dashboard() {

  // const [login, {error}] = useMutation(LO)

  return (
    <div className="dashboard">
          <h1>Your Dashboard</h1>
    </div>
  );
}
